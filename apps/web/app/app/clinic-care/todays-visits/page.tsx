"use client";

import { ClinicCareTrackboardView } from "@/features/clinic-care/ClinicCareTrackboardView";

/** MEDUI.D4C.2A — Today's Visits route inside unified Clinic shell. */
export default function ClinicCareTodaysVisitsPage() {
  return <ClinicCareTrackboardView mode="todaysVisits" />;
}
