"use client";

import { ClinicCareRegistrationView } from "@/features/clinic-care/ClinicCareRegistrationView";

/** MEDUI.D4C.3 — Clinic Care registration / appointment / walk-in workspace. */
export default function ClinicCareRegistrationPage() {
  return <ClinicCareRegistrationView />;
}
