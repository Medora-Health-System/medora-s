"use client";

import React from "react";

/**
 * Point d’entrée module Fracture (Haiti) — pas de workflow complet dans ce passage.
 */
export default function FracturePage() {
  return (
    <div style={{ maxWidth: 720, padding: "24px 16px" }}>
      <h1 style={{ marginTop: 0 }}>Fracture</h1>
      <p style={{ fontSize: 16, color: "#424242", lineHeight: 1.6 }}>
        Module en cours de finalisation.
      </p>
      <p style={{ fontSize: 14, color: "#757575" }}>
        Cette section sera reliée au parcours clinique existant lorsque le flux sera validé.
      </p>
    </div>
  );
}
