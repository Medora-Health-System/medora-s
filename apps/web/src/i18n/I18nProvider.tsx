"use client";import{createContext,useContext,useEffect,useState}from"react";import{messages,type MessageKey}from"./messages";import{canonicalEn,canonicalFr,platformText,translatePlatformText}from"./platformText";export type Locale="en"|"fr";const STORAGE="medora.locale";const I18n=createContext({locale:"en" as Locale,setLocale:(_l:Locale)=>{},t:(key:MessageKey)=>messages.en[key] as string,formatDate:(v:string|Date)=>new Intl.DateTimeFormat("en-US",{dateStyle:"medium",timeStyle:"short"}).format(new Date(v))});export function I18nProvider({children}:{children:React.ReactNode}){const[locale,setState]=useState<Locale>("en");useEffect(()=>{const saved=localStorage.getItem(STORAGE);if(saved==="fr"||saved==="en")setState(saved)},[]);function setLocale(value:Locale){localStorage.setItem(STORAGE,value);document.cookie=`medora_locale=${value}; Path=/; Max-Age=31536000; SameSite=Lax`;document.documentElement.lang=value;setState(value)}useEffect(()=>{document.documentElement.lang=locale;const root=document.body;const reverse=new Map(Object.entries(platformText.fr).map(([en,fr])=>[fr,en]));const canonicalReverse=new Map(Object.entries(canonicalFr).map(([en,fr])=>[fr,en]));const localize=(raw:string)=>{const exact=(reverse.get(raw)??canonicalReverse.get(raw)??raw);if(locale==="fr")return canonicalFr[exact]??translatePlatformText("fr",exact);return canonicalEn[exact]??exact};const visit=(node:Node)=>{if(node.nodeType===Node.TEXT_NODE&&node.textContent){const raw=node.textContent,trim=raw.trim();if(trim){let next=localize(trim);if(next===trim&&node.parentElement?.closest(".platform-error"))next=messages[locale]["common.unknownError"];if(next!==trim)node.textContent=raw.replace(trim,next)}}if(node instanceof HTMLElement){for(const attr of ["aria-label","placeholder","title"]){const raw=node.getAttribute(attr);if(raw){const next=localize(raw);if(next!==raw)node.setAttribute(attr,next)}}}node.childNodes.forEach(visit)};visit(root);const observer=new MutationObserver(records=>records.forEach(r=>r.addedNodes.forEach(visit)));observer.observe(root,{childList:true,subtree:true});return()=>observer.disconnect()},[locale]);const t=(key:MessageKey)=>messages[locale][key];const formatDate=(v:string|Date)=>new Intl.DateTimeFormat(locale==="fr"?"fr-FR":"en-US",{dateStyle:"medium",timeStyle:"short"}).format(new Date(v));return <I18n.Provider value={{locale,setLocale,t,formatDate}}>{children}</I18n.Provider>}export const useI18n=()=>useContext(I18n);export function LanguageSelector(){const{locale,setLocale,t}=useI18n();return <div role="group" aria-label={t("common.language")} className="language-selector"><button aria-pressed={locale==="en"} onClick={()=>setLocale("en")}>EN</button><span aria-hidden> | </span><button aria-pressed={locale==="fr"} onClick={()=>setLocale("fr")}>FR</button></div>}
