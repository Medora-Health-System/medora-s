"use client";

/**
 * Legacy export — provider lifecycle UI now lives in MedicationGovernanceManageModal.
 * Kept for import stability; renders nothing (section owns the manage entry point).
 */
export function MedicationOrderLifecyclePanel() {
  return null;
}
