/** M1.8B.7K.3 — unified facility MAR shift timeline is the primary MAR tab UI. */
export const MAR_TAB_UNIFIED_TIMELINE_ONLY = true;

/** Dev fallback: set true locally to show legacy pass queue / MAR table / history. */
export const MAR_TAB_SHOW_LEGACY_SECTIONS = !MAR_TAB_UNIFIED_TIMELINE_ONLY;
