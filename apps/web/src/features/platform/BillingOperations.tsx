"use client";import{useEffect,useState}from"react";import{platformFacilitiesApi,type FacilitySummary}from"@/lib/platform/api";type View="governance"|"claims"|"payments";export function BillingOperations(){const[facilities,setFacilities]=useState<FacilitySummary[]>([]),[id,setId]=useState(""),[view,setView]=useState<View>("governance"),[data,setData]=useState<any>(),[error,setError]=useState("");useEffect(()=>{platformFacilitiesApi.list().then(x=>{setFacilities(x);setId(x[0]?.id||"")})},[]);async function load(){try{setError("");const r=await fetch(`/api/backend/platform/operations/${view==='governance'?'billing':view}?facilityId=${encodeURIComponent(id)}`),body=await r.json();if(!r.ok)throw new Error(body.message||"Unable to load");setData(body)}catch(e){setError(e instanceof Error?e.message:"Unable to load")}}const facility=facilities.find(x=>x.id===id);return <><section className="platform-panel"><div className="platform-search"><label>Facility scope<select value={id} onChange={e=>{setId(e.target.value);setData(undefined)}}>{facilities.map(f=><option key={f.id} value={f.id}>{f.name} ({f.code})</option>)}</select></label><label>Revenue-cycle function<select value={view} onChange={e=>{setView(e.target.value as View);setData(undefined)}}><option value="governance">Billing governance</option><option value="claims">Claim submission</option><option value="payments">Payments & ERA</option></select></label><button className="button" onClick={()=>void load()} disabled={!id}>Load</button></div>{facility&&<p className="notice">Selected billing facility: <b>{facility.name}</b>. This does not grant care-workspace or chart authority.</p>}{error&&<p className="platform-error">{error}</p>}</section>{data&&view==="governance"&&<section className="platform-panel"><h2>Billing governance</h2><Metrics value={data.summary??data}/></section>}{data&&view!=="governance"&&<section className="platform-panel"><div className="panel-heading"><h2>{view==="claims"?"Claim submission queue":"Payments & ERA queue"}</h2><span className="platform-badge">{data.total} records</span></div><div className="table-wrap"><table><thead><tr>{Object.keys(data.rows?.[0]??{}).slice(0,7).map(k=><th key={k}>{human(k)}</th>)}</tr></thead><tbody>{(data.rows??[]).map((row:any,i:number)=><tr key={row.claimId??row.encounterId??i}>{Object.keys(data.rows[0]??{}).slice(0,7).map(k=><td key={k}>{render(row[k])}</td>)}</tr>)}</tbody></table></div></section>}</>}
function Metrics({value}:{value:any}){return <div className="metric-grid">{Object.entries(value??{}).filter(([,v])=>v==null||["string","number","boolean"].includes(typeof v)).map(([k,v])=><article key={k}><small>{human(k)}</small><strong>{render(v)}</strong></article>)}</div>}function render(v:unknown){if(v==null)return"—";if(typeof v==="boolean")return v?"Yes":"No";return String(v)}function human(v:string){return v.replace(/([a-z])([A-Z])/g,"$1 $2").replaceAll("_"," ")}
