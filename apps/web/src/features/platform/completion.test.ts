import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";const read=(p:string)=>readFileSync(new URL(`../../../${p}`,import.meta.url),"utf8");describe("D4SEC.1C.5A completion source boundaries",()=>{it("has no unavailable-state component or raw JSON operations dump",()=>{const ui=read("src/features/platform/PlatformUi.tsx"),projection=read("src/features/platform/OperationalProjection.tsx");expect(ui).not.toContain(["NOT YET","AVAILABLE"].join(" "));expect(projection).not.toContain("JSON.stringify")});it("exposes typed system routes",()=>{for(const route of ["health","backup","go-live"])expect(read(`app/platform/system/${route}/page.tsx`)).toContain("OperationalProjection")});it("keeps revenue cycle explicitly facility scoped",()=>{const billing=read("src/features/platform/BillingOperations.tsx");expect(billing).toContain("facilityId=");expect(billing).toContain("does not grant care-workspace or chart authority")});it("uses governed facility activation and MFA reset requests",()=>{expect(read("app/platform/facilities/[id]/page.tsx")).toContain('operationType:"FACILITY_ACTIVATION_CHANGE"');expect(read("src/features/platform/MfaRecoveryRequest.tsx")).toContain('operationType:"MFA_RESET"')});it("keeps TOTP ephemeral and retries through the reusable step-up flow",()=>{const step=read("src/features/platform/PlatformStepUp.tsx");expect(step).toContain("setCode(");expect(step).toContain("await action?.retry()");expect(step).not.toContain("localStorage");expect(step).not.toContain("sessionStorage")})});
