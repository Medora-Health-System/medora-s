"use client";import Link from "next/link";import {usePathname} from "next/navigation";import {usePlatform} from "./PlatformContext";import {visibleAreas,type PlatformArea} from "./access";
const meta:Record<PlatformArea,{label:string;icon:string}>={facilities:{label:"Facilities",icon:"▦"},staff:{label:"Medora Staff",icon:"◎"},security:{label:"Security",icon:"⌾"},compliance:{label:"Compliance",icon:"✓"},billing:{label:"Billing / RCM",icon:"$"},catalog:{label:"Catalog / Configuration",icon:"◇"},system:{label:"System Operations",icon:"⌁"}};
export function PlatformShell({children}:{children:React.ReactNode}){const {context}=usePlatform();const path=usePathname();return <div className="platform-shell"><aside className="platform-sidebar"><Link href="/platform" className="platform-brand"><span className="platform-brandmark">M</span><span><b>MEDORA</b><small>PLATFORM ADMIN</small></span></Link><nav aria-label="Platform Admin">{visibleAreas(context).map(a=><Link key={a} href={`/platform/${a}`} className={path.startsWith(`/platform/${a}`)?"active":""}><span aria-hidden>{meta[a].icon}</span>{meta[a].label}</Link>)}</nav><div className="platform-authority"><span className="status-dot"/>Authority verified<small>{context?.platformPrincipal?"Platform principal":context?.staff?.persona?.replaceAll("_"," ")}</small></div></aside><div className="platform-main"><header className="platform-top"><div><span className="eyebrow">Enterprise operations</span><strong>MEDORA PLATFORM ADMIN</strong></div><Link href="/app">Return to care workspace</Link></header>{children}</div></div>}
