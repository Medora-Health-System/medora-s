import{describe,expect,it}from"vitest";import{authorizedPlatformRedirect,filterPlatformFacilities,resolvePostLoginWorkspace}from"./operations";
const rows=[{id:"1",name:"North General",code:"NG-01",isActive:true,facilityType:"HOSPITAL",country:"Haiti",timezone:"America/Port-au-Prince"},{id:"2",name:"South Clinic",code:"SC-02",isActive:false,facilityType:"CLINIC",country:"Canada",timezone:"America/Toronto"}];
describe("platform operational boundaries",()=>{it("filters name and code case-insensitively with partial matching",()=>{expect(filterPlatformFacilities(rows,"GENERAL")).toHaveLength(1);expect(filterPlatformFacilities(rows,"c-0")).toHaveLength(1)});it("filters status and additional safe fields",()=>{expect(filterPlatformFacilities(rows,"clinic","INACTIVE")).toEqual([rows[1]]);expect(filterPlatformFacilities(rows,"hait")).toEqual([rows[0]])});it("accepts platform redirects only after authoritative entry",()=>{expect(authorizedPlatformRedirect("/platform/staff",true)).toBe("/platform/staff");expect(authorizedPlatformRedirect("/platform/staff",false)).toBeNull();expect(authorizedPlatformRedirect("//platform/evil",true)).toBeNull()});it("lands authoritative platform principal or active capable staff in platform while preserving facility and MSPP destinations",()=>{expect(resolvePostLoginWorkspace("/app/admin",null,true)).toBe("/platform");expect(resolvePostLoginWorkspace("/app/emergency/trackboard",null,true)).toBe("/platform");expect(resolvePostLoginWorkspace("/app/admin",null,false)).toBe("/app/admin");expect(resolvePostLoginWorkspace("/app/public-health/summary",null,false)).toBe("/app/public-health/summary")})});
