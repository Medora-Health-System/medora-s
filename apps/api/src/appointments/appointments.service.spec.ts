import { AppointmentsService } from "./appointments.service";

describe("AppointmentsService ambulatory guards (unit)", () => {
  it("module exports AppointmentsService", () => {
    expect(AppointmentsService).toBeDefined();
  });
});
