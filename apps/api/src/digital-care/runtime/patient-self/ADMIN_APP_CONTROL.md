# Facility configuration enforcement

Patient secure messaging checks the selected facility's canonical runtime configuration before any conversation data is read or written. Messaging requires the Digital Care module, Patient Portal messaging, secure messaging, and patient chat to be enabled for that facility.
