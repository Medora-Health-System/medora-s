# Phase 18 — Quality Guide

Scores (0–100): coverage, evidence completeness, review completeness, confidence calibration, governance completeness, audit completeness, traceability, explainability, reproducibility → composite **qualityScore**.

Computed via `calculateOperationalQualityScores` and persisted in `MedicationRecommendationQualitySnapshot`.
