# Source Registry Guide

Registry: `MK_EXPANSION_WAVE3_SOURCE_REGISTRY` in `medicationKnowledgeExpansionWave3.ts`.

Approval states: REGISTERED → VALIDATED → APPROVED_FOR_INGESTION → IMPORTED → ACTIVATED (or REJECTED).

APPLY requires `assertMkExpansionWave3SourceApprovedForIngestion`.
