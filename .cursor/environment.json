{
  "name": "Medora S (Cloud Agent dev)",
  "install": "bash .cursor/scripts/cloud-agent-install.sh",
  "start": "bash .cursor/scripts/cloud-agent-start.sh",
  "terminals": [
    {
      "name": "API (:3001)",
      "command": "pnpm --filter @medora/api dev",
      "description": "NestJS API dev server (watch mode) on http://localhost:3001. Reads apps/api/.env (disposable local Postgres)."
    },
    {
      "name": "Web (:3002)",
      "command": "PORT=3002 pnpm --filter @medora/web dev",
      "description": "Next.js web dev server on http://localhost:3002. Proxies to the API at http://localhost:3001."
    }
  ],
  "ports": [
    { "name": "api", "port": 3001 },
    { "name": "web", "port": 3002 }
  ]
}
